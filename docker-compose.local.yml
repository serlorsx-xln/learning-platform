# Local-only overlay (not used by Coolify).
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d

services:
  app:
    env_file: .env
    ports:
      - "${WEB_BIND:-0.0.0.0}:3000:3000"

  worker-andrewbiggs:
    env_file: .env

  worker-edlearning:
    env_file: .env

  worker-speexx:
    env_file: .env
